import { decodeURL, validateURL } from '@sudoku/sencode';

export default class SudokuWikiUrlDecoder {
	canHandle(input) {
		return validateURL(input);
	}

	decode(input) {
		return decodeURL(input);
	}

	getName() {
		return 'SudokuWikiUrlDecoder';
	}
}
