import { decodeSencode, validateSencode } from '@sudoku/sencode';

export default class SencodeDecoder {
	canHandle(input) {
		return validateSencode(input);
	}

	decode(input) {
		return decodeSencode(input);
	}

	getName() {
		return 'SencodeDecoder';
	}
}
