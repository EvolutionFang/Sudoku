import { writable } from 'svelte/store';

function createLogicalCandidates() {
    const { subscribe, set, update } = writable([]);

    return {
        subscribe,
        set, 
        update
    };
}

export const logicalCandidates = createLogicalCandidates();